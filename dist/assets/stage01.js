window.STAGE_ONE_HTML = {"zh": "<h2>1. 为什么需要工具？</h2>\n<p>我曾经在 ChatGPT 里询问如何配置服务器的静态 IP。整个过程几乎都是：<strong>它给我命令，我去执行，再把结果贴回来，由它判断下一步。</strong></p>\n<p>在这条链路里，我承担了不少机械的执行和搬运工作。那么，能不能让 LLM 直接获得执行结果，并继续完成任务，而不必每一步都由我来中转？</p>\n<p>这就是我理解的“给聊天模型加上手”——<strong>工具</strong>。Codex 已经能读取文件、修改代码和运行命令；背后并不是模型亲自执行代码，而是模型提出调用请求，由运行环境执行，再把结果交回模型。(<a href=\"https://developers.openai.com/codex/cli/\" target=\"_blank\" rel=\"noopener noreferrer\">OpenAI Developers ↗</a>)</p>\n<p>但“能执行”和“可靠地执行”是两回事。只看最基础的三个工具，就有不少问题：Read 面对巨大的日志文件，应该一次读多少？Edit 修改时，文件已经不同于刚才读取的版本，该怎么办？Bash 又该如何避免误删文件等危险操作？</p>\n<p>因此，<strong>工具可以从一个函数开始，但可靠的工具设计不止这个函数，还包括它的执行约束。</strong>这些约束不急着一次做完，我们先从最小版本出发，遇到问题，再逐步补上。</p>\n<h2>2. 什么是工具？四家官方文档的表述</h2>\n<p><em>以下为官方文档要点的中文转述，并非逐字引用。</em></p>\n<div class=\"article-table\"><table><thead><tr><th scope=\"col\">来源</th><th scope=\"col\">对工具的表述</th></tr></thead><tbody>\n<tr><th scope=\"row\"><strong>Anthropic / Claude</strong></th><td>模型可以调用开发者定义或平台提供的功能；模型生成结构化调用，由应用或平台执行。(<a href=\"https://platform.claude.com/docs/en/agents-and-tools/tool-use/overview\" target=\"_blank\" rel=\"noopener noreferrer\">Claude Platform ↗</a>)</td></tr>\n<tr><th scope=\"row\"><strong>OpenAI</strong></th><td>工具是提供给模型使用的能力；工具调用是模型使用该能力的请求，工具结果则是执行后返回给模型的信息。(<a href=\"https://developers.openai.com/api/docs/guides/function-calling\" target=\"_blank\" rel=\"noopener noreferrer\">OpenAI Developers ↗</a>)</td></tr>\n<tr><th scope=\"row\"><strong>LangChain</strong></th><td>工具是具有明确输入、输出的可调用函数，模型根据上下文决定何时调用，以及传入什么参数。(<a href=\"https://docs.langchain.com/oss/python/langchain/tools\" target=\"_blank\" rel=\"noopener noreferrer\">Docs by LangChain ↗</a>)</td></tr>\n<tr><th scope=\"row\"><strong>Google ADK</strong></th><td>工具是提供给 Agent 的特定能力，让它能够执行动作，与文本生成和推理之外的环境交互。(<a href=\"https://google.github.io/adk-docs/tools-custom/\" target=\"_blank\" rel=\"noopener noreferrer\">Google GitHub ↗</a>)</td></tr>\n</tbody></table></div>\n<h2>3. 从 Read 开始：我们的探索路线</h2>\n<p>我们以 <strong>Claude Code 的公开工具行为为参考</strong>，自行实现最小版本的 Read、Edit 和 Bash。先从 Read 开始，沿着下面这条路线逐步探索，而不是一次搭好完整工具系统。</p>\n<div class=\"article-table\"><table><thead><tr><th scope=\"col\">顺序</th><th scope=\"col\">要弄清楚的问题</th></tr></thead><tbody>\n<tr><th scope=\"row\"><strong>① 核心函数</strong></th><td>先写一个读取小型测试文本的函数：输入文件路径，如何得到文件内容？</td></tr>\n<tr><th scope=\"row\"><strong>② 读取约束</strong></th><td>文件太大怎么办？逐步增加起始行、读取行数和内容上限，并明确告诉模型是否读全。Claude Code 的 Read 提供了 <code>offset</code>、<code>limit</code> 和部分读取提示，可以作为参考。(<a href=\"https://code.claude.com/docs/en/tools-reference\" target=\"_blank\" rel=\"noopener noreferrer\">Claude ↗</a>)</td></tr>\n<tr><th scope=\"row\"><strong>③ 接入模型</strong></th><td>如何用工具名称、用途描述和参数定义，让模型知道怎样使用 Read？模型发出调用后，程序如何找到并执行对应函数？(<a href=\"https://platform.claude.com/docs/en/agents-and-tools/tool-use/implement-tool-use\" target=\"_blank\" rel=\"noopener noreferrer\">Claude Platform ↗</a>)</td></tr>\n<tr><th scope=\"row\"><strong>④ 执行前检查</strong></th><td>参数是否合法、路径是否允许访问？哪些检查属于工具自身，哪些适合通过 <code>PreToolUse</code> Hook 增加？(<a href=\"https://code.claude.com/docs/en/hooks\" target=\"_blank\" rel=\"noopener noreferrer\">Claude ↗</a>)</td></tr>\n<tr><th scope=\"row\"><strong>⑤ 执行后处理</strong></th><td>返回内容是否超长、是否需要脱敏？如何通过输出处理或 <code>PostToolUse</code> Hook，让模型拿到合适的结果？(<a href=\"https://code.claude.com/docs/en/hooks\" target=\"_blank\" rel=\"noopener noreferrer\">Claude ↗</a>)</td></tr>\n<tr><th scope=\"row\"><strong>⑥ 异常反馈</strong></th><td>文件不存在、权限不足、执行失败时，哪些信息交给模型调整操作，哪些由程序处理？(<a href=\"https://platform.claude.com/docs/en/agents-and-tools/tool-use/handle-tool-calls\" target=\"_blank\" rel=\"noopener noreferrer\">Claude Platform ↗</a>)</td></tr>\n</tbody></table></div>\n<h3>这里先明确两个边界</h3>\n<p><strong>预校验不等于 Hook。</strong>参数校验、工具自身的约束和权限控制，与 Hook 是不同机制。Hook 是在特定时机插入额外逻辑的扩展点，不是所有检查的统称。后置 Hook 也不是“撤销按钮”：它可以处理结果或提供反馈，但不能撤销已经发生的文件修改或命令执行。(<a href=\"https://code.claude.com/docs/en/hooks\" target=\"_blank\" rel=\"noopener noreferrer\">Claude ↗</a>)</p>\n<p><strong>错误也不应该全部原样塞给 LLM。</strong>我们自己的最小版本，可以先采用这样的分工：</p>\n<ul><li><strong>需要模型调整操作的错误</strong>：例如路径不存在、读取范围不合法，返回清晰原因和可采取的下一步；权限拒绝可以告知，但不能引导模型绕过。官方工具调用协议支持将失败作为工具结果返回。(<a href=\"https://platform.claude.com/docs/en/agents-and-tools/tool-use/handle-tool-calls\" target=\"_blank\" rel=\"noopener noreferrer\">Claude Platform ↗</a>)</li><li><strong>需要运行时处理的故障</strong>：远程工具的网络、限流问题先由程序按策略有限重试，最终失败再返回摘要。这个本地文件 Read 的最小版本不引入网络；<strong>模型 API 自身请求失败，也不能误报成 Read 执行失败</strong>。</li><li><strong>不应直接暴露的信息</strong>：密钥、凭证和无关内部细节不返回；必要的失败原因则应保留，避免模型误以为操作成功。这里将脱敏作为我们自己的设计要求，不能只依赖提示词。</li></ul>", "en": "<h2>1. Why do we need tools?</h2>\n<p>I once asked ChatGPT how to configure a static IP address on a server. The process was almost always the same: <strong>it gave me commands, I ran them, pasted the results back, and let it decide what to do next.</strong></p>\n<p>I was doing a lot of mechanical execution and copying. Could the LLM receive the results directly and continue the task, without having me relay every step?</p>\n<p>This is how I understand giving a chat model “hands”: <strong>tools</strong>. Codex can already read files, edit code, and run commands. The model does not execute the code itself: it requests a tool call, the runtime executes it, and the result goes back to the model. (<a href=\"https://developers.openai.com/codex/cli/\" target=\"_blank\" rel=\"noopener noreferrer\">OpenAI Developers ↗</a>)</p>\n<p>But being able to execute and executing reliably are different. Even the three most basic tools raise questions: how much of a huge log should Read load at once? What should Edit do if a file has changed since it was read? How should Bash avoid dangerous actions such as accidentally deleting files?</p>\n<p>So <strong>a tool can start as a function, but reliable tool design also includes constraints on its execution.</strong> We do not need to implement every constraint immediately. We can start small and add them as we encounter problems.</p>\n<h2>2. What is a tool? Four official descriptions</h2>\n<p><em>These are paraphrases of the main points in the official documentation, not direct quotations.</em></p>\n<div class=\"article-table\"><table><thead><tr><th scope=\"col\">Source</th><th scope=\"col\">Description of tools</th></tr></thead><tbody>\n<tr><th scope=\"row\"><strong>Anthropic / Claude</strong></th><td>A model can call developer-defined or platform-provided functionality. It produces a structured call that the application or platform executes. (<a href=\"https://platform.claude.com/docs/en/agents-and-tools/tool-use/overview\" target=\"_blank\" rel=\"noopener noreferrer\">Claude Platform ↗</a>)</td></tr>\n<tr><th scope=\"row\"><strong>OpenAI</strong></th><td>A tool is a capability available to the model; a tool call is a request to use that capability; a tool result is information returned to the model after execution. (<a href=\"https://developers.openai.com/api/docs/guides/function-calling\" target=\"_blank\" rel=\"noopener noreferrer\">OpenAI Developers ↗</a>)</td></tr>\n<tr><th scope=\"row\"><strong>LangChain</strong></th><td>A tool is a callable function with defined inputs and outputs. The model uses the context to decide when to call it and which arguments to supply. (<a href=\"https://docs.langchain.com/oss/python/langchain/tools\" target=\"_blank\" rel=\"noopener noreferrer\">Docs by LangChain ↗</a>)</td></tr>\n<tr><th scope=\"row\"><strong>Google ADK</strong></th><td>A tool gives an Agent a specific capability to act and interact with environments beyond text generation and reasoning. (<a href=\"https://google.github.io/adk-docs/tools-custom/\" target=\"_blank\" rel=\"noopener noreferrer\">Google ADK ↗</a>)</td></tr>\n</tbody></table></div>\n<h2>3. Starting with Read: our exploration path</h2>\n<p>We will use <strong>Claude Code's publicly documented tool behavior as a reference</strong> to build minimal versions of Read, Edit, and Bash ourselves. We will start with Read and work through the following questions gradually.</p>\n<div class=\"article-table\"><table><thead><tr><th scope=\"col\">Step</th><th scope=\"col\">Question to explore</th></tr></thead><tbody>\n<tr><th scope=\"row\"><strong>① Core function</strong></th><td>Start with a function that reads a small test file: given its path, how do we obtain its contents?</td></tr>\n<tr><th scope=\"row\"><strong>② Reading limits</strong></th><td>What if the file is too large? Gradually add a starting line, a line count, and a content limit, and tell the model whether it received the entire file. Claude Code's Read offers <code>offset</code>, <code>limit</code>, and partial-read notices as references. (<a href=\"https://code.claude.com/docs/en/tools-reference\" target=\"_blank\" rel=\"noopener noreferrer\">Claude ↗</a>)</td></tr>\n<tr><th scope=\"row\"><strong>③ Model integration</strong></th><td>How do the name, description, and parameter definitions teach the model to use Read? Once the model requests a call, how does the program locate and execute the function? (<a href=\"https://platform.claude.com/docs/en/agents-and-tools/tool-use/implement-tool-use\" target=\"_blank\" rel=\"noopener noreferrer\">Claude Platform ↗</a>)</td></tr>\n<tr><th scope=\"row\"><strong>④ Before execution</strong></th><td>Are the arguments valid, and is access to the path allowed? Which checks belong to the tool itself, and which could be added through a <code>PreToolUse</code> hook? (<a href=\"https://code.claude.com/docs/en/hooks\" target=\"_blank\" rel=\"noopener noreferrer\">Claude ↗</a>)</td></tr>\n<tr><th scope=\"row\"><strong>⑤ After execution</strong></th><td>Is the output too long, and does it need redaction? How can output processing or a <code>PostToolUse</code> hook give the model an appropriate result? (<a href=\"https://code.claude.com/docs/en/hooks\" target=\"_blank\" rel=\"noopener noreferrer\">Claude ↗</a>)</td></tr>\n<tr><th scope=\"row\"><strong>⑥ Failure feedback</strong></th><td>When a file is missing, access is denied, or execution fails, what should the model receive so it can adjust, and what should the program handle? (<a href=\"https://platform.claude.com/docs/en/agents-and-tools/tool-use/handle-tool-calls\" target=\"_blank\" rel=\"noopener noreferrer\">Claude Platform ↗</a>)</td></tr>\n</tbody></table></div>\n<h3>Two boundaries to establish first</h3>\n<p><strong>Pre-validation is not the same as a hook.</strong> Argument validation, tool constraints, permission control, and hooks are different mechanisms. A hook is an extension point for adding logic at a particular time, not a general name for every check. An after-execution hook is not an undo button: it can process results or provide feedback, but it cannot reverse a file change or command that has already executed. (<a href=\"https://code.claude.com/docs/en/hooks\" target=\"_blank\" rel=\"noopener noreferrer\">Claude ↗</a>)</p>\n<p><strong>Not every raw error should be passed to the LLM.</strong> For our own minimal implementation, we can begin with this division:</p>\n<ul><li><strong>Errors requiring the model to change its action:</strong> for example, a missing path or an invalid read range. Return a clear reason and a possible next step. A permission denial can be reported, but the model must not be guided to bypass it. The official tool protocol supports returning a failure as a tool result. (<a href=\"https://platform.claude.com/docs/en/agents-and-tools/tool-use/handle-tool-calls\" target=\"_blank\" rel=\"noopener noreferrer\">Claude Platform ↗</a>)</li><li><strong>Failures the runtime should handle:</strong> for network or rate-limit issues in remote tools, the program first applies a bounded retry policy and returns a summary if it still fails. This minimal local-file Read tool does not introduce networking. <strong>A failed request to the model API must not be reported as a Read execution failure.</strong></li><li><strong>Information that should not be exposed directly:</strong> keys, credentials, and unrelated internal details should be omitted. Keep the necessary failure reason so the model does not mistake failure for success. Redaction is our own design requirement here and cannot rely on a prompt alone.</li></ul>"};
